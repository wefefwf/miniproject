<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springbootsmini.app.mapper.ProductMapper">

    <!-- 상품 등록 -->
  <insert id="addProduct" parameterType="com.springbootsmini.app.domain.Product">
    INSERT INTO product(
        category_id, 
        product_name, 
        price, 
        image_url, 
        image_url2, -- 추가
        image_url3, -- 추가
        description, 
        rating, 
        recall_status, 
        ingredient_check
    )
    VALUES(
        #{category_id}, 
        #{product_name}, 
        #{price}, 
        #{image_url}, 
        #{image_url2}, -- 추가
        #{image_url3}, -- 추가
        #{description}, 
        #{rating}, 
        #{recall_status}, 
        #{ingredient_check}
    )
</insert>

	    <!-- 단일 상품 조회 -->
<select id="getProduct" parameterType="int" resultType="com.springbootsmini.app.domain.Product">
    SELECT 
        product_id, category_id, product_name, price, 
        image_url, 
        image_url2, -- 추가
        image_url3, -- 추가
        description, rating, recall_status, ingredient_check,
        ingredients, nutrition, origin_manufacturer  
    FROM product
    WHERE product_id = #{product_id}
</select>
    
    <!-- 카테고리별 상품 목록 조회 -->
<select id="getProductList" resultType="com.springbootsmini.app.domain.Product">
    SELECT * FROM product 
    <where>
        <if test="keyword == null or keyword == ''">
            AND category_id = #{category_id}
        </if>
        
        <if test="keyword != null and keyword != ''">
            AND (product_name LIKE CONCAT('%', #{keyword}, '%') 
                 OR description LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </where>
    ORDER BY product_id DESC
</select>
    
    <!--카테고리 리스트 -->
    <select id="getCategoryList" resultType="com.springbootsmini.app.domain.Category">
        SELECT 
            category_id, 
            category_name, 
            description 
        FROM product_category
        ORDER BY category_id ASC
    </select>
    
    <!-- product에 넣음 -->
<update id="updateProduct" parameterType="com.springbootsmini.app.domain.Product">
    UPDATE product 
    SET 
        category_id = #{category_id},
        product_name = #{product_name},
        price = #{price},
        image_url = #{image_url},
        image_url2 = #{image_url2}, -- 추가
        image_url3 = #{image_url3}, -- 추가
        description = #{description},
        rating = #{rating},
        recall_status = #{recall_status},
        ingredient_check = #{ingredient_check}
    WHERE product_id = #{product_id}
</update>

    <delete id="deleteProduct" parameterType="int">
        DELETE FROM product 
        WHERE product_id = #{product_id}
    </delete>

</mapper>
